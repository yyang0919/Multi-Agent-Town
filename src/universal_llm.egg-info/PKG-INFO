Metadata-Version: 2.4
Name: universal-llm
Version: 0.1.0
Summary: A simple universal LLM wrapper based on LangChain.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: langchain>=0.3.0
Requires-Dist: langchain-core>=0.3.0
Requires-Dist: langchain-openai>=0.2.0
Requires-Dist: python-dotenv>=1.0.0

# 基于 LangChain 的通用 LLM 调用项目详细说明

> 如果你只想快速了解项目，请先看根目录的 `README.md`。  
> 本文档用于说明：**环境搭建、vLLM 部署、项目结构、工作流程、运行方法**。

---

## 1. 项目简介

本项目实现了一个基于 **LangChain** 的通用 LLM 调用封装，目标是：

- 用统一接口调用不同供应商模型；
- 支持 **LangChain 原生 provider**；
- 支持 **OpenAI-compatible provider**；
- 支持通过 **vLLM** 调用本地模型；
- 当前聚焦文本对话与流式输出，暂不接入 tool。

这个项目适合作为：

- 课程作业；
- LangChain 封装练习；
- 本地 vLLM + Python 接口联调示例；
- 后续扩展 Agent / RAG / Tool 的基础工程。

---

## 2. 当前项目结构

本项目已经改成标准 **src-layout** 结构，当前目录如下：

```text
.
├─ src/
│  └─ universal_llm/
│     ├─ __init__.py
│     ├─ config.py
│     ├─ factory.py
│     ├─ messages.py
│     └─ client.py
├─ scripts/
│  ├─ run_vllm_example.py
│  ├─ run_openai_example.py
│  └─ check_env.py
├─ examples/
│  ├─ basic_chat.py
│  └─ stream_chat.py
├─ tests/
│  └─ test_config.py
├─ pyproject.toml
├─ requirements.txt
├─ README.md
└─ README_langchain_vllm.md
```

### 各目录作用

#### `src/universal_llm/`
核心源码目录：

- `config.py`：配置类、provider 注册信息、响应结构、异常类、LangChain 延迟加载
- `factory.py`：根据配置创建底层 chat model
- `messages.py`：消息构造、role 标准化、消息转换、文本提取
- `client.py`：主类 `UniversalLLM`
- `__init__.py`：统一导出接口

#### `scripts/`
用于直接运行的脚本：

- `run_vllm_example.py`：本地 vLLM 调用示例
- `run_openai_example.py`：OpenAI-compatible 云端示例
- `check_env.py`：环境检查

#### `examples/`
最小教学示例：

- `basic_chat.py`
- `stream_chat.py`

#### `tests/`
测试目录：

- `test_config.py`

---

## 3. 当前支持的 provider

为了更适合作业展示，当前只保留最有代表性的 provider。

### 3.1 LangChain 原生 provider

- `openai`
- `anthropic`
- `google_genai`
- `ollama`

### 3.2 OpenAI-compatible provider

- `deepseek`
- `openai_compatible`
- `vllm`
- `lm_studio`
- `local_openai`

其中：

- `deepseek` 代表云端 OpenAI-compatible 服务；
- `vllm` / `lm_studio` / `local_openai` 代表本地 OpenAI-compatible 服务。

---

## 4. 安装方式

由于项目采用的是标准 `src-layout`，推荐安装方式如下。

### 4.1 安装依赖

```powershell
pip install -r requirements.txt
```

### 4.2 以可编辑模式安装项目

在项目根目录执行：

```powershell
pip install -e .
```

执行后，就可以直接在任意脚本中使用：

```python
from universal_llm import LLMConfig, UniversalLLM
```

而不需要再手动修改 `sys.path`。

---

## 5. 本地搭建 vLLM 环境

本项目的完整流程是：

**先部署 vLLM → 再使用本项目调用本地模型**

---

### 5.1 下载 vLLM

```powershell
git clone https://github.com/vllm-project/vllm.git
cd vllm
```

---

### 5.2 启动并登录 Docker

请先启动 Docker Desktop，并登录 Docker。

可以先检查 Docker 是否正常：

```powershell
docker version
```

---

### 5.3 构建 vLLM CPU 镜像

在 `vllm` 目录下执行：

```powershell
docker build --progress=plain --build-arg max_jobs=4 -f .\docker\Dockerfile.cpu -t vllm-cpu --shm-size=4g .
```

命令说明：

- `-f .\docker\Dockerfile.cpu`：使用 CPU 版 Dockerfile
- `-t vllm-cpu`：将镜像命名为 `vllm-cpu`
- `--build-arg max_jobs=4`：设置构建并行数
- `--shm-size=4g`：设置共享内存
- `.`：当前目录作为构建上下文

构建完成后可以检查：

```powershell
docker images
```

---

### 5.4 登录 Hugging Face 并创建 Token

vLLM 第一次启动并加载模型时，需要从 Hugging Face 下载模型，所以你需要：

1. 登录 Hugging Face  
2. 创建 Access Token  
3. 在 PowerShell 中设置环境变量

例如：

```powershell
$env:HUGGING_FACE_HUB_TOKEN="你的 Hugging Face Token"
```

> 不要把 Token 直接写进源码。

---

### 5.5 启动 vLLM，并自动下载模型

执行：

```powershell
docker run -it --rm -p 8000:8000 -v "${env:USERPROFILE}\.cache\huggingface:/root/.cache/huggingface" --env "HUGGING_FACE_HUB_TOKEN=$env:HUGGING_FACE_HUB_TOKEN" vllm-cpu --model Qwen/Qwen2.5-1.5B-Instruct --dtype float16
```

该命令会：

- 启动 vLLM 服务
- 将容器 8000 端口映射到本机 8000
- 自动下载并加载 `Qwen/Qwen2.5-1.5B-Instruct`
- 使用本机 Hugging Face 缓存

---

### 5.6 验证 vLLM 是否启动成功

另开一个 PowerShell 窗口执行：

```powershell
curl http://127.0.0.1:8000/v1/models
```

如果返回模型信息，说明服务已启动成功。

本项目后续连接的地址就是：

```text
http://127.0.0.1:8000/v1
```

---

## 6. 项目工作流程

### 第一步：部署模型服务

通过 Docker 启动 vLLM，本地暴露 OpenAI-compatible API。

### 第二步：编写统一配置

通过 `LLMConfig` 配置：

- provider
- model
- api_key
- base_url
- temperature
- max_tokens

### 第三步：自动路由

项目内部根据配置自动选择：

- native provider → `init_chat_model(...)`
- openai-compatible provider → `ChatOpenAI(...)`

### 第四步：统一调用

通过 `UniversalLLM` 完成：

- 普通对话
- 流式输出
- 异步调用
- 历史消息拼接

---

## 7. 核心代码设计

### 7.1 `config.py`

负责：

- `LLMConfig`
- `ResolvedLLMConfig`
- `LLMResponse`
- provider 注册信息
- 配置校验
- 异常类
- LangChain 运行时加载

### 7.2 `factory.py`

负责根据配置创建 LangChain chat model。

### 7.3 `messages.py`

负责：

- 消息结构统一
- role 标准化
- 历史消息转换
- 输出文本提取

### 7.4 `client.py`

对外统一接口：

- `chat()`
- `generate()`
- `stream()`
- `achat()`
- `agenerate()`
- `astream()`

---

## 8. 如何运行项目

> 运行前请确保已执行：
>
> ```powershell
> pip install -r requirements.txt
> pip install -e .
> ```

### 8.1 运行本地 vLLM 示例

```powershell
python scripts/run_vllm_example.py
```

### 8.2 运行云端 OpenAI-compatible 示例

先设置环境变量：

```powershell
$env:DEEPSEEK_API_KEY="你的 DeepSeek API Key"
```

再执行：

```powershell
python scripts/run_openai_example.py
```

### 8.3 运行环境检查

```powershell
python scripts/check_env.py
```

### 8.4 运行教学示例

```powershell
python examples/basic_chat.py
python examples/stream_chat.py
```

### 8.5 运行测试

```powershell
python tests/test_config.py
```

---

## 9. 示例代码

### 9.1 vLLM 基础调用

```python
from universal_llm import LLMConfig, UniversalLLM

config = LLMConfig.for_vllm(
    model="Qwen/Qwen2.5-1.5B-Instruct",
    base_url="http://127.0.0.1:8000/v1",
    temperature=0.2,
)

client = UniversalLLM(config)
print(client.chat("请简要介绍一下 Transformer。"))
```

### 9.2 流式输出

```python
for token in client.stream("请分三点介绍 LangChain"):
    print(token, end="", flush=True)
```

---

## 10. 项目特点

本项目的特点：

1. 统一接口  
2. 同时支持本地与云端  
3. 同时支持 native provider 与 OpenAI-compatible  
4. 标准 `src-layout` 工程结构  
5. 适合课程作业展示  
6. 易于后续扩展

---

## 11. 当前限制

当前版本暂不包含：

- Tool calling
- Agent
- RAG
- 自动记忆管理
- 多模型自动路由策略

---

## 12. 课程汇报建议顺序

如果你要做课程汇报，建议按下面顺序讲：

1. 项目目标  
2. 标准 `src-layout` 工程结构  
3. vLLM 部署流程  
4. LangChain 统一封装设计  
5. 本地模型调用演示  
6. 云端兼容接口调用演示  
7. 总结与扩展方向

---

## 13. 参考资料

- vLLM GitHub：https://github.com/vllm-project/vllm
- vLLM Docker 部署文档：https://docs.vllm.ai/en/latest/deployment/docker/
- Hugging Face：https://huggingface.co/

---

## 14. 总结

本项目现在已经是一个标准的 Python `src-layout` 工程：

- 核心代码位于 `src/universal_llm/`
- 通过 `pip install -e .` 安装使用
- 支持本地 vLLM 与云端 OpenAI-compatible 服务
- 使用 LangChain 统一封装模型调用

对于课程作业来说，这样的结构兼顾了：

- 清晰性
- 完整性
- 可运行性
- 可扩展性
